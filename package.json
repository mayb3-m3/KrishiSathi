{
  "name": "krishisathi",
  "version": "1.0.0",
  "private": true,
  "description": "Integrated Agriculture & Rural Livelihood Platform for Bangladesh",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
